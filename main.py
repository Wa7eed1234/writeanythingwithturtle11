import turtle

# Create a turtle object
t = turtle.Turtle()

# Set the turtle's speed to a slow speed
t.speed(0)

# Use the turtle's pen to draw the letters of "abdallah"

t.goto(-100, 0)
t.pendown()
t.write("fuck you omar", font=("Arial", 20, "bold"))

# Close the turtle window
turtle.done()